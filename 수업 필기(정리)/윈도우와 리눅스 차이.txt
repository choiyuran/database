윈도우
- 경로를 적을때 기호로 역슬러시(\)
- 설정된 환경변수를 참조할때 앞뒤로 %표시(%JAVA_HOME%\bin)
- ;으로 문구 구별(PATH=PATH;%JAVA_HOME%\bin;)

리눅스 
- 경로를 적을때 그냥 슬러시(/)
- 설정된 환경변수를 참조할때 앞에 $표시($ORACLE_HOME)
- :으로 문구 구별(PATH=$PATH:$ORACLE_HOME/bin:$JAVA_HOME/bin)

